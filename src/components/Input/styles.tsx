import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    label: {
        color: '#fff',
        fontSize: 16,
        marginBottom: 5,
    },
      input: {
        color: '#fff',
        borderWidth: 1,
        borderColor: '#ddd',
        padding: 10,
        marginBottom: 10,
        borderRadius: 4,
    },
});